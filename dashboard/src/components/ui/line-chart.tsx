import React from 'react';

export const LineChart = ({ className, ...props }) => (
  <div className={`h-64 w-full bg-muted ${className}`} {...props}>
    {/* Placeholder for line chart */}
  </div>
);